# ── Core data stack ────────────────────────────────────────────────────────
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
pyyaml>=6.0        # params.yaml loading (also a transitive dep of dvc/mlflow)

# ── Data ingestion ──────────────────────────────────────────────────────────
yfinance>=0.2
fredapi>=0.5   # NB: fredapi is versioned 0.x on PyPI — the earlier '>=3.1' pin never existed and broke every fresh install
BVCscrap>=0.1        # BVC (Casablanca) prices via medias24
lxml>=5.0            # required by BVCscrap
python-dotenv>=1.0   # loads FRED_API_KEY from .env in pipeline.py
requests>=2.31       # Bank Al-Maghrib reference-rate API (src/bam_fx.py). Was
                     # already present transitively via yfinance/fredapi; now a
                     # DIRECT dependency, so it is declared rather than assumed.

# ── Data validation ─────────────────────────────────────────────────────────
pandera>=0.20

# ── Analytical query engine ──────────────────────────────────────────────────
duckdb>=0.10

# ── Statistical modelling (Phase 2+) ────────────────────────────────────────
statsmodels>=0.14
scipy>=1.12
scikit-learn>=1.4

# ── ML models (Phase 3-4) ────────────────────────────────────────────────────
hmmlearn>=0.3
arch>=6.0

# ── Supervised ML signal models (Phase 4B / F7) ──────────────────────────────
xgboost>=2.0
# torch is deliberately NOT listed here: an LSTM signal strategy was built
# and tested in isolation, then dropped from this pass after torch +
# xgboost loaded together in the same process segfaulted on this machine
# (a native/OpenMP library conflict, not a code defect in either library).
# Deferred to a run on more capable hardware — see CLAUDE.md's Phase 4B notes.

# ── Experiment tracking ──────────────────────────────────────────────────────
mlflow>=2.12

# ── Pipeline versioning ──────────────────────────────────────────────────────
dvc>=3.49

# ── Orchestration / scheduling ────────────────────────────────────────────────
dagster>=1.8
dagster-webserver>=1.8

# ── Notebooks & EDA ─────────────────────────────────────────────────────────
jupyter>=1.0
notebook>=7.0
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0

# ── Dashboard + API (Phase 6 + 7 — merged "Portfolio ML Suite") ─────────────
streamlit>=1.36
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27               # FastAPI TestClient + Page 2's API client
